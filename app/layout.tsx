import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/layout/Navbar";
import Footer from "@/components/layout/Footer";

export const metadata: Metadata = {
  title: "MovieBox - Stream Movies & TV Shows",
  description: "Your ultimate destination for streaming movies and TV shows online. Discover new movies, watch trailers, and add reviews.",
  keywords: ["movies", "streaming", "TV shows", "film reviews", "cinema"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="antialiased">
        <Navbar />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
